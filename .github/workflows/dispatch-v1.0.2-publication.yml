name: "[archived] Dispatch v1.0.2 publication"

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Archived: v1.0.2 is already public."
          echo "This workflow performs no writes."
