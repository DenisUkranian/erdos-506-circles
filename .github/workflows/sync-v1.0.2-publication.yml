name: "[archived] Synchronize v1.0.2 publication metadata"

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Archived: v1.0.2 metadata was synchronized manually after publication."
          echo "This workflow performs no writes."
