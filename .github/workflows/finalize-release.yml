name: "[archived] Finalize v1.0.0 release"

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Archived: v1.0.0 is a historical release and must not be marked Latest."
          echo "This workflow performs no writes."
