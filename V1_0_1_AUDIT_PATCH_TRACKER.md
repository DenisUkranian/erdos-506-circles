# v1.0.1 audit patch tracker

The automated `publish-audited-v1.0.1-final` workflow builds and verifies the audited source/metadata patch release. The release preserves `Erdos506.zip` byte-for-byte and publishes only after all asset hashes and the canonical quick proof replay pass.
